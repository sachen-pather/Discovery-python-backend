# Core dependencies (you already have these)
flask==2.3.3
flask-cors==4.0.0
pandas==2.1.1
openai==1.3.7
python-dotenv==1.0.0

# PDF processing
PyMuPDF==1.23.8

# Missing dependencies from your code:
groq==0.4.1              # For Groq LLM provider in llm_categorizer.py
numpy==1.24.3            # Used extensively in pandas operations
requests==2.31.0         # Often needed by other libraries

# For CSV parsing robustness
# (pandas handles most CSV needs, but your code mentions papaparse equivalent)

# For production deployment
gunicorn==21.2.0