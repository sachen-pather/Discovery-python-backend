# Core dependencies (you probably already have these)
flask==2.3.3
flask-cors==4.0.0
pandas==2.1.1
openai==1.3.7
python-dotenv==1.0.0

# New dependency for PDF processing
PyMuPDF==1.23.8

# Optional: for more advanced PDF processing if needed
# pdfplumber==0.9.0
# tabula-py==2.8.2

# For production deployment (optional)
# gunicorn==21.2.0